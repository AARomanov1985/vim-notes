# to create a link:
sudo chmod +x goal
sudo ln -s $(pwd)/goal /usr/bin/
# to create syntax in vim:
sudo cp note.vim /usr/share/vim/vim74/syntax/
echo "au BufNewFile,BufRead *.note set filetype=note" >> ~/.vimrc
